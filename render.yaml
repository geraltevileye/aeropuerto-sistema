services:
  - type: web
    name: aeropuerto-sistema
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sistema_3szc
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
